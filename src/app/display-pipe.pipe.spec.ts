import { DisplayPipePipe } from './display-pipe.pipe';

describe('DisplayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
